__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.env
*.sqlite
.DS_Store
.firecrawl/
.claude/scheduled_tasks.lock
.worktrees

# LOCG cache: retired as the source of truth (BUI-93). The collection +
# wish-list are now served from the gixen server's collection-store (BUI-87);
# data/locg/ is at most a local, non-authoritative working cache, so the whole
# directory is ignored — no more 1.8 MB collection.json churn in git.
data/locg/
